"""The `__about__` module exposes the version of the `results` package:

Example:

```python
from results.__about__ import __version__
print(__version__)
```

Note that container version can also be imported directly from `results` package:

Example:

```python
from results import __version__
print(__version__)
```
"""
__version__ = "0.1.0"
